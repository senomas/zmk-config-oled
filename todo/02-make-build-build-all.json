{
  "meta": {
    "nn": 2,
    "name": "make-build-build-all",
    "goal": "Add BUILD variable to control which firmware `make build` builds (default: nice_corne_left/right_oled), and add `make build_all` to build everything"
  },
  "tasks": [
    {
      "title": "Add BUILD variable with default firmware selection",
      "done": true,
      "detail": "Insert the following block right before the `ZMK_WORKSPACE :=` line (before the 'CI-aligned codebase target' comment block):\n\n```makefile\n# Default firmware to build (space-separated artifact names from build.yaml).\n# Set to empty to build all, or list specific artifacts (e.g. nice_corne_left_oled).\nBUILD ?= nice_corne_left_oled nice_corne_right_oled\n```\n\nThis uses `?=` so it can be overridden by env or command line (e.g. `make build BUILD=\"nice_settings_reset\"`).",
      "children": [
        {
          "implemented": true,
          "id": "40c93432",
          "note": "Inserted `BUILD ?= nice_corne_left_oled nice_corne_right_oled` before the ZMK_WORKSPACE line. Uses `?=` for env/CLI override support.",
          "reviews": [
            {
              "text": "BUILD variable at lines 187-189, positioned right before ZMK_WORKSPACE := as specified. Uses ?= for env/CLI override support. Default values (nice_corne_left_oled nice_corne_right_oled) match artifact-names in build.yaml. Comment explains usage clearly.",
              "gap": false,
              "id": "6ed203c7",
              "resolved": false,
              "children": []
            }
          ],
          "children": []
        }
      ],
      "id": "00bd108d"
    },
    {
      "title": "Add filter logic in build target to skip non-BUILD artifacts",
      "done": true,
      "detail": "In the `build:` target, two changes:\n\n1. Change the first shell line from:\n   `@python3 scripts/parse-build-yaml.py | jq -c '.[]' | while IFS= read -r entry; do \\`\n   to:\n   `@build_filter=\"$(BUILD)\"; \\`\n   `python3 scripts/parse-build-yaml.py | jq -c '.[]' | while IFS= read -r entry; do \\`\n\n2. After the line `artifact=$$(echo \"$$artifact\" | tr \" \" \"_\"); \\` and before `snippet_flag=\"\"; \\`, insert:\n\n```makefile\n\t\tif [ -n \"$$build_filter\" ]; then \\\n\t\t\tcase \" $$build_filter \" in \\\n\t\t\t\t*\" $$artifact \"*) ;; \\\n\t\t\t\t*) echo \"Skipping $$artifact (not in BUILD)\"; continue ;; \\\n\t\t\tesac; \\\n\t\tfi; \\\n```\n\nThe double-escaping (`$$build_filter`, `$$artifact`) is required because these are shell variables inside a make recipe. The `case` pattern uses spaces around both the filter list and the artifact name to ensure exact word matching (e.g. `nice_corne_left` won't accidentally match `nice_corne_left_oled`).",
      "children": [
        {
          "implemented": true,
          "id": "d053feb3",
          "note": "Changed build target: prepended `build_filter=\"$(BUILD)\"` variable, added case-match filter block after artifact name normalization. Uses `\" $$build_filter \"` / `\" $$artifact \"` space-padded pattern for exact word matching.",
          "reviews": [
            {
              "text": "Filter logic correctly implemented at lines 205 and 212-217. build_filter=\"$(BUILD)\" set before the parsing loop. The case-match block uses space-padded strings (\" $$build_filter \" / \" $$artifact \") for exact word matching, preventing partial matches (e.g. nice_corne_left won't match nice_corne_left_oled). Prints \"Skipping $$artifact (not in BUILD)\" and continues when filtered out. When BUILD is empty, [ -n \"$$build_filter\" ] is false and the filter block is skipped entirely.",
              "gap": false,
              "id": "6b96c36d",
              "resolved": false,
              "children": []
            }
          ],
          "children": []
        }
      ],
      "id": "6738cc9a"
    },
    {
      "title": "Add build_all target",
      "done": true,
      "detail": "Insert the following block right before `### CODEBASE_UROB START`:\n\n```makefile\n# Build all firmware defined in build.yaml (no filter)\nbuild_all:\n\t$(MAKE) build BUILD=\"\"\n```\n\nWhen BUILD is empty, `[ -n \"$$build_filter\" ]` is false, so the filter block is skipped entirely and everything in build.yaml gets built.",
      "children": [
        {
          "implemented": true,
          "id": "35ad20b7",
          "note": "Added `build_all:` target before `### CODEBASE_UROB START` that invokes `$(MAKE) build BUILD=\"\"` to build everything with no filter.",
          "reviews": [
            {
              "text": "build_all target at lines 244-246, positioned right before ### CODEBASE_UROB START. Uses $(MAKE) build BUILD=\"\" to recursively invoke build with an empty filter, which bypasses the filter block entirely and builds everything in build.yaml. Correct pattern.",
              "gap": false,
              "id": "5e955e16",
              "resolved": false,
              "children": []
            }
          ],
          "children": []
        }
      ],
      "id": "4c803b31"
    },
    {
      "title": "Update help text descriptions for build and build_all",
      "done": true,
      "detail": "In the `help:` target, update the 'Primary targets (CI-aligned):' section:\n\n1. Change the `build` description from:\n   `@echo \"  build          Build all firmware defined in build.yaml (what CI builds)\"`\n   to:\n   `@echo \"  build          Build selected firmware (BUILD var, default: nice_corne_left/right_oled)\"`\n\n2. Add a new line after the `build` description:\n   `@echo \"  build_all      Build all firmware defined in build.yaml\"`",
      "children": [
        {
          "implemented": true,
          "id": "7d5dd3a7",
          "note": "Updated build help line to mention BUILD variable and default. Added build_all help line.",
          "reviews": [
            {
              "text": "Help text updated at lines 38-39. Build description now reads \"Build selected firmware (BUILD var, default: nice_corne_left/right_oled)\" — accurately reflects the BUILD variable behavior. build_all line added: \"Build all firmware defined in build.yaml\". Both match the spec.",
              "gap": false,
              "id": "708cb458",
              "resolved": false,
              "children": []
            }
          ],
          "children": []
        }
      ],
      "id": "3bf23f43"
    }
  ],
  "version": "1.0"
}
